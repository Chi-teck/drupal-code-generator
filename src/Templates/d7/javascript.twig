/**
 * Core behavior for {{ name }} {{ project_type }}.
 */
(function ($) {
  Drupal.behaviors.{{ machine_name }} = {
    attach: function (context, settings) {

      alert('It works!');

    }
  }
}(jQuery));
