/**
 * Implements hook_user_categories().
 */
function {{ machine_name }}_user_categories() {
  return array(array(
    'name' => 'account',
    'title' => t('Account settings'),
    'weight' => 1,
  ));
}
