/**
 * Implements hook_insert().
 */
function {{ machine_name }}_insert($node) {
  db_insert('mytable')
    ->fields(array(
      'nid' => $node->nid,
      'extra' => $node->extra,
    ))
    ->execute();
}
