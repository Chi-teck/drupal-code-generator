/**
 * Implements HookBootExitTestCase::getInfo().
 */
public static function getInfo() {
  return array(
    'name' => 'Boot and exit hook invocation',
    'description' => 'Test that hook_boot() and hook_exit() are called correctly.',
    'group' => 'Bootstrap',
  );
}
