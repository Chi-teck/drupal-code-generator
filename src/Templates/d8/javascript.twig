/**
 * @file
 * {{ name }} behaviors.
 */

(function ($, Drupal) {

  'use strict';

  /**
   * Example behavior description.
   */
  Drupal.behaviors.{{ machine_name }}Example = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (jQuery, Drupal));
