<?php

/**
 * @file
 * Contains Drupal\{{ machine_name }}\{{ class_prefix }}ListBuilder.
 */

namespace Drupal\{{ machine_name }};

use Drupal\Core\Entity\EntityInterface;
use Drupal\Core\Entity\EntityListBuilder;

/**
 * Provides a list controller for {{ entity_type_label|lower }} entity type.
 */
class {{ class_prefix }}ListBuilder extends EntityListBuilder {

  /**
   * {@inheritdoc}
   *
   * @DCG: Optional.
   */
  public function render() {
    $build['table'] = parent::render();
    $build['summary']['#markup'] = $this->t('Total records: @total');
    return $build;
  }

  /**
   * {@inheritdoc}
   */
  public function buildHeader() {
    $header['id'] = $this->t('ID');
    $header['title'] = $this->t('Title');
    $header['status'] = $this->t('Status');
    $header['uid'] = $this->t('Author');
    $header['created'] = $this->t('Created');
    $header['changed'] = $this->t('Updated');
    return $header + parent::buildHeader();
  }

  /**
   * {@inheritdoc}
   */
  public function buildRow(EntityInterface $entity) {
    /* @var $entity \Drupal\{{ machine_name }}\Entity\{{ class_prefix }} */
    $row['id'] = $entity->id();
    $row['title'] = $entity->link();
    $row['status'] = $entity->link();
    $row['uid'] = $entity->link();
    $row['created'] = $entity->getCreatedTime();
    $row['changed'] = $entity->link();
    return $row + parent::buildRow($entity);
  }

}
