# Local overrides of Behat configuration.
imports:
  - behat.yml
default:
  extensions:
    Behat\MinkExtension:
      base_url: 'http://localhost'
    Drupal\DrupalExtension:
      drush:
        alias: 'local'
