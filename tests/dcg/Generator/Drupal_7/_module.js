/**
 * Example behaviors.
 */
(function ($) {

  /**
   * Behavior description.
   */
  Drupal.behaviors.example = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  }

}(jQuery));
