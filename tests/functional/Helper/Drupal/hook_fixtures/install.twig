<?php

declare(strict_types=1);

/**
 * @file
 * Install, update and uninstall functions for the {{ name }} module.
 */

/**
 * Implements hook_install().
 */
function {{ machine_name }}_install($is_syncing): void {
  // Set general module variables.
  \Drupal::state()->set('mymodule.foo', 'bar');
}
