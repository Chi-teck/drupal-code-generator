<VirtualHost *:80>
  ServerName {{ hostname }}
  ServerAlias www.{{ hostname }}
  DocumentRoot {{ docroot }}

  <Directory {{ docroot }}>
    Options All
    AllowOverride All
    Order allow,deny
    Allow from all
  </Directory>

</VirtualHost>
