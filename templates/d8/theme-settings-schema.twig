# Schema for the configuration files of the {{ name }} theme.
{{ machine_name }}.settings:
  type: theme_settings
  label: '{{ name }} settings'
  mapping:
    font_size:
      type: integer
      label: Font size
