name: {{ name }}
type: module
description: {{ description }}
package: {{ package }}
version: {{ version }}
core: 8.x
{% if dependencies %}
dependencies:
{% for dependency in dependencies %}
  - {{ dependency|trim|lower }}
{% endfor %}
{% endif %}
