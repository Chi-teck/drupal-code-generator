/**
 * Implements hook_user_logout().
 */
function {{ machine_name }}_user_logout($account) {
  db_insert('logouts')
    ->fields([
      'uid' => $account->id(),
      'time' => time(),
    ])
    ->execute();
}
