/**
 * Implements hook_path_insert().
 */
function {{ machine_name }}_path_insert($path) {
  db_insert('mytable')
    ->fields([
      'alias' => $path['alias'],
      'pid' => $path['pid'],
    ])
    ->execute();
}
