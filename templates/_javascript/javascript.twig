/**
 * @file
 * {{ name }} behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.{{ behavior }} = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
