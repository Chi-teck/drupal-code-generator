{% import '@lib/di.twig' as di %}
services:
  logger.{{ service_id }}:
    class: Drupal\{{ machine_name }}\Logger\{{ class }}
{% if services %}
    arguments: [{{ di.arguments(services) }}]
{% endif %}
    tags:
      - { name: logger }
