{{ route_name }}:
  path: '{{ route_path }}'
  defaults:
    _title: '{{ route_title }}'
    _controller: '\Drupal\{{ machine_name }}\Controller\{{ class }}'
  requirements:
    _permission: '{{ route_permission }}'
