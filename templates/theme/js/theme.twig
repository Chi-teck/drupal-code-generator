/**
 * @file
 * {{ name }} behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.{{ machine_name|camelize(false) }} = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
