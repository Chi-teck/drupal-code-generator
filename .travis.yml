language: php

php:
  - 7.2
  - 7.1
  - 7.0
  - 5.6

env:
  matrix:
  - DEPENDENCIES_LOWEST=0
  - DEPENDENCIES_LOWEST=1

before_script:
  - composer install
  - [ $DEPENDENCIES_LOWEST -eq 1 ] && composer update --prefer-lowest
  - composer global require "phpunit/phpunit" "^5.7"

script: $(composer config home)/vendor/bin/phpunit
